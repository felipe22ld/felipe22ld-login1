import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'felipe22ld-login',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
